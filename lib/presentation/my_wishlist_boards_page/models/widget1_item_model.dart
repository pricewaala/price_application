import '../../../core/app_export.dart';/// This class is used in the [widget1_item_widget] screen.
class Widget1ItemModel {Widget1ItemModel({this.image, this.id, }) { image = image  ?? ImageConstant.imgImage9;id = id  ?? ""; }

String? image;

String? id;

 }
